html
  head 
    title="MyBank"
    link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet")
  body
    div(class="container")
      h2(class="pb-2 border-bottom")="MyBank"
        if isLoggedIn
            small 
              a(href="/logout")="Logout"
            div.navbar.navbar-expand.navbar-light
                ul.navbar-nav.mb-2
                    li.nav-item
                        a.nav-link(href="/") User Management
                    li.nav-item.dropdown
                        li.nav-item
                            a.nav-link(href="/") Create New User
                        li.nav-item
                            a.nav-link(href="/") Change Password
                        li.nav-item
                            a.nav-link(href="/") Update Email
        else
            a(href="/login")="Login"
                
      //div(class="row")
        div(class="col")
          a(href="/balance")="View Account Balance"

        div(class="col")
          a(href="/account")="View Account Number"

        div(class="col")
          a(href="/contact")="Contact Us"